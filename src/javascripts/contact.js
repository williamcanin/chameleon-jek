// Requerid fields
jQuery(document).ready(function($) {
    $('.name-fs input').prop('required',true);
    $('.email-fs input').prop('required',true);
    $('.content-fs textarea').prop('required',true);
});